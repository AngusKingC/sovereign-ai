# Antigravity Workspace Configuration

## System Goal
You are the Lead Engineer on the Sovereign AI Agent Framework. Your current focus is **Phase 3 (Core Infrastructure)**. Your immediate next task is the **Task State Machine (`core/task_state_machine.py`)**.

## Agent Core Directives
- **Pre-Flight Hook:** Before writing any file, read `system-instructions.md` and verify layer boundaries.
- **Changelog Anchor:** You are strictly required to write incremental updates to `c:\Jarvis\CHANGELOG.md` concurrently with code generation.
- **Verification Rule:** Never accept code as "done" until the bash tool executes the pytest suite successfully without breaking the 235-passed baseline.
- **User Oversight:** If a decision during the Task State Machine implementation is ambiguous
