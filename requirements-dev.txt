# Development dependencies — install with: pip install -r requirements-dev.txt
# These are NOT required for runtime. They are required for:
#   - Pre-commit hooks (detect-secrets, pre-commit, black, isort, mypy, ruff)
#   - Test coverage (pytest-cov)
#   - Dead code analysis (vulture)
#   - CI pipeline (all of the above)

detect-secrets>=1.5.0
pre-commit>=3.6.0
pytest-cov>=4.1.0
vulture>=2.11

# Pre-commit hook dependencies (referenced by .pre-commit-config.yaml)
black>=24.3.0
isort>=5.13.0
mypy>=1.9.0
ruff>=0.3.5

# Type stubs for mypy (referenced by .pre-commit-config.yaml additional_dependencies)
types-requests
types-PyYAML
